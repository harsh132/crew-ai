{
  "name": "@edgerouter/crew-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply crew-backend --local",
    "migrate:remote": "wrangler d1 migrations apply crew-backend --remote",
    "deploy": "wrangler deploy",
    "privy-check": "bun --env-file=.dev.vars privy-check.ts"
  },
  "dependencies": {
    "@privy-io/node": "0.34.0",
    "viem": "2.56.3"
  }
}
